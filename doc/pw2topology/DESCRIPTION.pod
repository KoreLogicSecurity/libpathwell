
=head1 DESCRIPTION

This utility reads one or more passwords from the command line
and/or a user-specified file (B<-f>), converts them to topology
strings using the default or a user-specified token set (B<-s>),
and writes the resultant values to stdout.

